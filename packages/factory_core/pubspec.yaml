name: factory_core
description: Typed factories, scoped resolution and deterministic ownership without Flutter.
version: 0.1.0
environment:
  sdk: '>=3.3.0 <4.0.0'
dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
